global:
  scrape_interval: 15s
scrape_configs:
  - job_name: airflow
    static_configs: [{ targets: ["airflow-webserver:8080"] }]
  - job_name: api
    metrics_path: /api/health
    static_configs: [{ targets: ["api:8000"] }]
